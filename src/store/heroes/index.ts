import * as types from './types';
import * as actions from './actions';
import { HeroesState, HeroesAction } from './reducer';

export {
    types,
    actions,
    HeroesState,
    HeroesAction,
};
